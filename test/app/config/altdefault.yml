imports:
    - { resource: config.yml }

pmg_pheanstalk:
    default_connection: another
    connections:
        another:
            host: anotherHost
            port: 1111
            timeout: 10
            persist: true
